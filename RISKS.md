# TubeFilm — Phase 2 예상 리스크 기록

> Phase 2는 **실제 브라우저 검증 없이 구현**되었음. 본 문서는 발생 가능한 리스크를 사전 기록하여, 추후 실제 테스트 중 문제 발생 시 "예상했던 사항"임을 확인하기 위한 용도.
>
> **작성 시점**: Phase 2 구현 직전 (2026-05-18)
> **검증 환경 부재 사유**: 회사 컴퓨터 Chrome 익스텐션 실행 제한

---

## R1. 풀스크린 진입 시 오버레이 / 패널 위치 어긋남

**증상 가설**
- 풀스크린 진입 후 영상 위에 그레인/스캔라인 오버레이가 안 보이거나, 영상 영역 밖으로 밀려남
- 패널이 풀스크린 화면에 안 보이고 풀스크린 종료 시에만 다시 보임

**원인**
- 풀스크린은 `fullscreenElement` 하위만 표시됨. 패널은 `document.body` 자식이라 풀스크린 밖.
- 풀스크린 진입 시 YouTube가 비디오의 부모 컨테이너를 바꿀 수 있음 → 기존 overlay가 stale parent에 남음

**대응 계획 (Phase 2 구현 시)**
- `fullscreenchange` 이벤트로 비디오/패널 재부착
- 패널을 `fullscreenElement`의 자식으로 이동, 종료 시 body로 복귀

**남는 리스크**
- WebKit/Moz 접두사 미고려 (Chrome만 타겟이라 영향 적음)
- YouTube `f` 키 단축키 풀스크린 vs 자체 시어터 모드 구분 실패 가능

---

## R2. 시어터 모드 / 미니 플레이어 추적 실패

**증상 가설**
- 시어터 모드(`t` 키)에서 비디오 크기가 바뀌어도 오버레이가 이전 크기 유지
- 미니 플레이어 진입 시 오버레이가 새 위치를 못 따라감

**원인**
- YouTube 클래스명(`ytp-big-mode`, `miniplayer`, `ytp-theater-mode-active`)이 **자주 바뀜**
- 본 구현은 2026-05 시점 클래스명 추정으로 작성

**대응 계획**
- `ResizeObserver`로 비디오 크기 변화 감지 → 오버레이는 비디오 부모 inset:0이라 자동 추종 기대
- 클래스명 변경 시 셀렉터 의존 코드 (예: `.ad-showing`)만 깨짐

**남는 리스크**
- YouTube가 미래에 셀렉터 바꾸면 광고 감지 / 시어터 감지 실패 (silent fail)

---

## R3. 광고 감지 부정확

**증상 가설**
- 광고 영상에서 "광고에 필터 끄기" 옵션이 동작 안 함
- 광고 끝나도 필터 복원 안 됨
- 일반 영상의 스폰서십 구간(채널 내 광고)은 광고로 감지 안 됨 (정상 동작)

**원인**
- `.ad-showing`은 `#movie_player`에 붙는 YouTube 내부 클래스. 변경 가능
- `MutationObserver`의 attributeFilter는 정확히 잡지만 초기 상태 누락 가능

**대응 계획**
- 옵저버 등록 직후 1회 상태 확인
- 광고 시작/종료 시 `applyCurrent()` 재호출

**남는 리스크**
- iframe 광고는 content script 닿지 않음 (`all_frames: false`)

---

## R4. PiP 모드에서 필터 미적용

**증상 가설**
- PiP 진입 시 필터/오버레이 사라짐 (예상된 동작)
- "PiP에선 필터 적용 안 됨" 토스트가 안 뜨거나 너무 빨리 사라짐

**원인**
- PiP는 OS 레벨 윈도우라 CSS filter / DOM 오버레이가 닿지 않음 (기술적 한계)

**대응 계획**
- `enterpictureinpicture` 이벤트에 토스트 표시 (안내 목적)

**남는 리스크**
- 토스트 표시 시점이 영상 위라 사용자 혼란 가능

---

## R5. 윈도우 리사이즈 시 패널 클램프 부작용

**증상 가설**
- 사용자가 의도적으로 패널을 모서리에 붙여놨는데, 리사이즈마다 안쪽으로 튐
- 듀얼 모니터에서 외부 모니터로 옮길 때 패널이 갑자기 이동

**원인**
- `window.resize` 핸들러에서 무조건 viewport 안으로 강제 이동

**대응 계획**
- 패널이 viewport 밖일 때만 안쪽으로 클램프
- 디바운스 200ms 적용

**남는 리스크**
- 외부 모니터로 옮긴 직후 한 번 위치 흔들림

---

## R6. 첫 진입 UX 가이드 노출 타이밍

**증상 가설**
- 페이지 로드 직후 패널이 안 보일 때 가이드만 빈 화면에 뜸
- 가이드 닫아도 다시 뜸 (저장 실패)

**원인**
- `chrome.storage.sync` 응답 전에 마운트 시도
- `hintDismissed` 저장 실패 시 매번 표시

**대응 계획**
- `loadState()` 완료 후에만 가이드 표시
- 저장 실패 시 in-memory flag fallback

**남는 리스크**
- 저장소 동기화 지연으로 다른 기기에서 가이드 재표시 (1회 정도는 허용 가능)

---

## R7. 그레인 해상도 동적 조정 — 메모리 사용

**증상 가설**
- 4K 영상에서 그레인 해상도 512로 올리면 toDataURL 호출 비용 큼
- 영상 크기 변할 때마다 재생성 → 메모리 누수

**원인**
- 노이즈 dataURL을 캐시하지만 크기 변경마다 재생성 시 이전 캐시는 GC 대기

**대응 계획**
- 해상도 변경 시에만 재생성, 동일 크기 영상 연속 시청 시 캐시 유지
- 최대 256으로 제한 (PLAN.md 권장값)

**남는 리스크**
- 그레인 패턴이 영상 해상도 따라 보이는 입자 크기 달라짐 (의도된 동작)

---

## R8. `chrome.storage` 실패 시 UX

**증상 가설**
- 시크릿 모드에서 `chrome.storage.sync`가 quota error 던짐
- 사용자가 설정 바꿔도 새로고침하면 사라짐

**원인**
- 시크릿 모드 / 동기화 비활성 / quota 초과

**대응 계획**
- try/catch 후 토스트로 "저장소 접근 실패, 세션 동안만 유지" 안내
- in-memory state는 유지

**남는 리스크**
- 사용자가 토스트 못 보면 설정이 휘발되는 줄 모름

---

## R9. content script 초기화 타이밍

**증상 가설**
- YouTube SPA 첫 진입 시 패널 안 뜸, 새로고침해야 뜸
- 매우 느린 네트워크에서 video 엘리먼트 없을 때 init 실패

**원인**
- `document_idle` 시점이지만 YouTube는 idle 후에도 영상 마운트 지연
- `findVideo()`가 null이면 `applyCurrent` 조기 종료

**대응 계획**
- `MutationObserver`로 비디오 등장 감시 (이미 구현됨)
- 진단용 console.log 추가 (디버그 빌드)

**남는 리스크**
- 사용자 체감 지연 0.5~1초

---

## R10. 셀렉터 의존 항목 일괄 갱신 필요

**증상 가설**
- 어느 날 갑자기 광고 감지 / 시어터 감지 / 비디오 탐지 모두 실패

**원인**
- YouTube DOM 리뉴얼 (분기마다 발생)

**대응 계획**
- 본 문서에 의존 셀렉터 명시 → 변경 시 일괄 수정

**의존 셀렉터 목록 (2026-05 시점)**
- `video.html5-main-video` (비디오 탐지 — fallback: 단순 `video`)
- `#movie_player` (광고 컨테이너 상위)
- `.ad-showing` (광고 활성 상태)
- `.ytp-fullscreen` (풀스크린)
- `.ytp-big-mode` (시어터 모드)
- `.miniplayer` 또는 `.ytp-miniplayer-ui` (미니 플레이어)

---

## 확인 방법

추후 사용자 로컬 테스트에서 위 증상 중 하나라도 발견되면:

1. 이 문서의 해당 R번호 인용
2. 실제 증상이 예상과 일치하는지 확인
3. 대응 계획 vs 실제 코드의 갭 점검

예상 외 증상은 본 문서에 추가하여 다음 세션 핸드오프 자료로 사용.

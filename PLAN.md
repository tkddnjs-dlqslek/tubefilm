# TubeFilm — 구현 계획 (Phase별)

## Phase 1 — MVP (✅ 코드 완성 / ⚠️ 브라우저 테스트 미진행)

### 완료된 작업
- [x] `manifest.json` — MV3, `*://*.youtube.com/*` 호스트 권한, `storage` 권한
- [x] `content/presets.js` — 6개 프리셋 정의 (filter / overlay / sliders)
- [x] `content/filter.js` — CSS filter 빌더 + 4종 오버레이 div 관리 + canvas 노이즈 생성
- [x] `content/panel.js` — 플로팅 패널 (드래그 / 슬라이더 / 프리셋 선택 / ON-OFF / 접기)
- [x] `content/content.js` — 진입점, `chrome.storage.sync` 로드/저장, 비디오 탐지, SPA 라우팅 추적
- [x] `content/styles.css` — 패널 + 4 오버레이 레이어 스타일 (각자 mix-blend-mode)
- [x] `README.md` — 개발자 모드 로드 안내

### 사용자가 검증해야 할 항목 (다음 세션 첫 단계)

1. [ ] `tubefilm/` 디렉토리를 Chrome 개발자 모드로 로드 → 로드 에러 없는지
2. [ ] YouTube 영상 페이지 진입 → 우상단 패널 표시 확인
3. [ ] 프리셋 6개 각각 클릭 → 영상 색감 변화 확인
4. [ ] 슬라이더 조정 → 실시간 반영 확인
5. [ ] ON/OFF 토글 → 필터 해제/재적용 확인
6. [ ] 페이지 새로고침 → 마지막 설정 복원 확인 (chrome.storage.sync)
7. [ ] 다른 영상으로 이동 (YouTube SPA) → 새 영상에 필터 자동 적용 확인
8. [ ] 패널 드래그 → 위치 이동 + 저장 확인
9. [ ] 접기 버튼 → 헤더만 남는지 확인

### 알려진 미해결 (Phase 2 이후)

- 풀스크린 모드 진입 시 오버레이 위치 어긋날 수 있음
- 시어터 모드 / 미니 플레이어 전환 시 비디오 컨테이너 변경 미추적
- 광고 영상에도 필터 적용 (의도된 동작, 추후 옵션화)
- PiP는 OS 레벨이라 CSS 닿지 않음 (안내만 필요)

---

## Phase 2 — 엣지 케이스 / UX 안정화

### 작업 목록

1. **풀스크린 대응**
   - `document.fullscreenchange` 리스너 추가
   - 풀스크린 진입 시: 비디오 부모가 그대로면 OK, 다르면 오버레이 재부착
   - 풀스크린 진입 시 패널 위치 재계산 (풀스크린 엘리먼트에 부착해야 보임)

2. **시어터 모드 / 미니 플레이어**
   - `#movie_player` 클래스 변화 감지 (`.ytp-fullscreen`, `.ytp-big-mode`, `.miniplayer` 등)
   - 비디오 컨테이너 크기 변경 시 오버레이 재계산 (`ResizeObserver`)

3. **광고 구간 처리**
   - YouTube 광고 감지: `.ad-showing` 클래스 또는 `video.ad-active` 변화
   - 옵션 토글: "광고에는 필터 끄기" — 패널에 체크박스 추가
   - 기본값은 광고에도 적용 (사용자 결정 우선)

4. **PiP 안내**
   - `picture-in-picture` 이벤트 감지 시 패널에 "PiP 모드에서는 필터 미적용" 토스트

5. **윈도우 리사이즈 대응**
   - 패널이 화면 밖으로 나가면 자동 클램프
   - `window.resize` 리스너로 재조정

6. **첫 진입 UX**
   - 새 사용자가 패널을 봤을 때 어디부터 만져야 할지 안내 (간단한 하이라이트 또는 툴팁)
   - "프리셋을 선택하세요" 힌트 외에 짧은 가이드 추가 검토

7. **성능**
   - 4K 영상에서 오버레이 + filter 성능 측정
   - 필요 시 그레인 해상도 동적 조정 (1080p 이하면 128x128, 그 이상은 256x256)
   - 패널 슬라이더 입력 → applyCurrent 호출 throttle 검토

8. **에러 견고성**
   - YouTube DOM 구조 변경 대응: `video` 태그 자체로 fallback (현재 구현됨, 견고성 검증)
   - `chrome.storage` 실패 시 in-memory fallback (현재 try/catch만, UI 알림 없음)

---

## Phase 3 — 출시 준비

### 작업 목록

1. **아이콘**
   - 16x16, 48x48, 128x128 PNG
   - 디자인 컨셉: 필름 릴(reel) + 컬러 → 모노 그라데이션, 미니멀
   - 임시로 SVG 생성 후 PNG 변환 가능 (canvas 기반 자동 생성도 옵션)

2. **팝업 (선택)**
   - `popup.html` — 간단한 on/off + 현재 프리셋 표시
   - 또는 인페이지 패널 한 가지로 유지 (사용자 결정 필요)
   - 결정: 인페이지 패널만 유지 추천 (단순성)

3. **스토어 자산**
   - 스크린샷 5장 (1280×800 또는 640×400)
     1. YouTube 영상 + 패널 (뉴진스 디지캠 적용)
     2. 민희진 필름 적용 비교
     3. 필름 누아르 적용 비교
     4. 80년대 VHS 적용 비교 (스캔라인 잘 보이게)
     5. 슬라이더 조정 UI 클로즈업
   - 프로모 타일 (440×280) — 로고 + 슬로건
   - 마키 (1400×560) — 선택 사항

4. **스토어 설명 (한·영)**
   - 짧은 설명 (132자 한국어): "YouTube 영상에 영화 같은 필름 톤을 입히세요. 뉴진스 디지캠, 필름 누아르, 80년대 VHS 등 6가지 프리셋."
   - 자세한 설명: 프리셋 6개 각각 한두 줄 + 슬라이더 안내 + 권한 설명

5. **`PRIVACY.md`** (Web Store 필수)
   - "데이터 수집·전송 없음. 모든 설정은 Chrome 동기화 저장소에만 저장됨."
   - 영문 버전 필수

6. **카테고리 / 키워드**
   - 카테고리: Photos / Productivity 중 선택 (Photos가 더 적합)
   - 키워드: youtube, filter, cinematic, film, grain, vintage, sepia, vhs, retro

7. **빌드/배포 스크립트**
   - `tubefilm/` → zip으로 패키징 (Web Store 업로드용)
   - 버전 번호 자동 증가 스크립트 (선택)

8. **README 업데이트**
   - 스크린샷 추가
   - Web Store 링크 (게시 후)
   - Contributing 가이드

---

## Phase 4 — (선택) 확장 기능

추후 사용자 피드백 따라 결정:

- 사용자 커스텀 프리셋 저장 (슬라이더 값 + 이름)
- 채널/영상별 자동 적용 (특정 채널 항상 흑백 등)
- 시간대별 자동 전환 (저녁 8시 후 자동 필름 톤)
- 다른 플랫폼 확장 (Twitch, Vimeo, 네이버 TV)
- 영상 진행률에 따라 점진적 효과
- WebGL 셰이더로 진짜 색수차/빛 번짐 (현재 CSS 한계)
- 단축키 (필터 빠르게 전환)

---

## 우선순위 / 권장 순서

새 세션은 이 순서로:

1. **검증** — Phase 1 사용자 테스트 (위 체크리스트 9개 항목)
2. **버그 수정** — 테스트에서 발견된 즉시 수정 필요한 것
3. **Phase 2** — 풀스크린 / 시어터 모드부터 (가장 흔한 사용 시나리오)
4. **Phase 2** — 광고 처리 / 리사이즈 / 성능
5. **Phase 3** — 아이콘 + 스토어 자산
6. **Phase 3** — 스토어 등록 (사용자가 Web Store 개발자 계정 보유 가정)
